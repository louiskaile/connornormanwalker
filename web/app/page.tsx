// The stories index is the site's landing page. Keep the canonical entry point
// at `/` while retaining `/stories` for backwards-compatible deep links.
export { default } from "./stories/page.tsx";
